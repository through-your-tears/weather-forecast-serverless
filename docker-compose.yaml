version: '3.3'

services:
  web:
    build:
      context: .
    volumes:
      - '.:/app'
      - '/app/node_modules'
    ports:
      - '80:80'
  redis:
    image: redis:5-alpine
    ports:
      - "6379:6379"